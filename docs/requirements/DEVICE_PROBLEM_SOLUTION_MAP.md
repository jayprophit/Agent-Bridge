# Cross-Device / Consumer Compute — Canonical Map (DEV-001..DEV-150, 100%)

Source: canonical owner audit (150 numbered titles in JSON). SOLVED_V08
where the profiler/router/tests prove it (capability probing, state
modeling, placement transparency, sensor-permission gates, raw-deletion);
PARTIAL_V08 for platform fragmentation/power/thermal/handoff mitigations;
ARCHITECTURE_READY for presentation/handoff/identity designs;
EXTERNAL_PLATFORM where OS vendors/stores own the behavior (updates,
sideloading, sensor accuracy, DRM, notches).
